<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flores.nico.catholicprayers" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="DATABSE"
            android:value="catholicprayers.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.flores.nico" />

        <activity
            android:name="com.flores.nico.catholicprayers.HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.flores.nico.catholicprayers.ViewPrayerActivity"
            android:label="@string/title_view_prayer"
            android:parentActivityName="com.flores.nico.catholicprayers.HomeActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.flores.nico.catholicprayers.HomeActivity" />
        </activity>
        <activity
            android:name="com.flores.nico.catholicprayers.EditPrayerActivity"
            android:label="@string/title_activity_edit_prayer"
            android:parentActivityName="com.flores.nico.catholicprayers.HomeActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.flores.nico.catholicprayers.HomeActivity" />
        </activity>
    </application>

</manifest>
